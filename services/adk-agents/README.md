# Google ADK specialists

Reserved for Google ADK agents running on Vertex AI. Each specialist exposes a typed `analyze(input) -> result` interface and `/health`. Gemini configuration is intentionally a placeholder until a GCP project and model policy are supplied.
