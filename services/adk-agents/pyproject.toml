[project]
name = "workeros-adk-agents"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["google-adk[gcp]>=2.0.0,<3.0.0", "google-genai>=1.0.0", "pydantic>=2.0", "fastapi>=0.115", "uvicorn>=0.30", "httpx>=0.27"]
[project.optional-dependencies]
test = ["pytest>=8.0"]

[tool.setuptools]
py-modules = ["agents", "app"]
