[project]
name = "workeros-adk-agents"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["google-adk>=0.1.0", "pydantic>=2.0", "fastapi>=0.115"]
[project.optional-dependencies]
test = ["pytest>=8.0"]
